{
  "name": "agent-swarm",
  "version": "3.0.0",
  "description": "Multi-agent swarm — Redis + single deployment",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ioredis": "^5.3.2",
    "node-cron": "^3.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
